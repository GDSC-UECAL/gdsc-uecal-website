---
import HeaderLink from "./HeaderLink.astro";
import { Image } from "astro:assets";
import Logo from "../assets/gdsclogo.svg";
import Button from "./Button.astro";
---

<!-- Flowbite Javascript -->
<link
  href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.css"
  rel="stylesheet"
/>
<script
  src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.js"
></script>

<nav class="fixed w-full top-0 z-50 transition-all" id="navbar">
  <div
    class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
    <a
      href="/"
      class="flex-shrink-0 flex items-center space-x-3 rtl:space-x-reverse">
      <Image src={Logo} alt="GDSC Logo" class="w-14 h-auto" />
    </a>

    <button
      data-collapse-toggle="navbar-default"
      type="button"
      class="inline-flex items-center p-2 w-10 h-10 justify-center text-md
		text-gray-500 rounded-lg md:hidden hover:bg-gray-500 focus:outline-none focus:ring-2 focus:ring-gray-200 dark:text-gray-400
		dark:hover:bg-gray-700 dark:focus:ring-gray-600"
      aria-controls="navbar-default"
      aria-expanded="false">
      <span class="sr-only">Open main menu</span>
      <svg
        class="w-6 h-6 text-white"
        aria-hidden="true"
        fill="none"
        viewBox="0 0 17 14">
        <path
          stroke="currentColor"
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M1 1h15M1 7h15M1 13h15"></path>
      </svg>
    </button>

    <div class="hidden w-full md:block md:w-auto" id="navbar-default">
      <ul
        class="font-medium flex flex-col gap-y-5 p-4 md:p-0 mt-4 text-dark rounded-lg md:flex-row md:space-x-8 md:mt-0 text-center">
        <HeaderLink
          href="/"
          class="block text-dark rounded md:bg-transparent md:p-0 dark:text-white">
          Home
        </HeaderLink>
        <HeaderLink
          href="/about"
          class="block text-dark rounded md:bg-transparent md:p-0 dark:text-white">
          About
        </HeaderLink>
        <HeaderLink
          href="/events"
          class="block text-dark rounded md:bg-transparent md:p-0 dark:text-white">
          Events
        </HeaderLink>
        <Button asLink={true} href="/about" class="block">
          Become a member
        </Button>
      </ul>
    </div>
  </div>
</nav>

<script>
  window.addEventListener("scroll", () => {
    const navbar: HTMLElement | null = document.getElementById("navbar");

    if (navbar) {
      if (window.scrollY > 0) {
        navbar.classList.add("bg-purple-950/60");
        navbar.classList.add("backdrop-filter");
        navbar.classList.add("backdrop-blur-lg");
        navbar.classList.add("bg-opacity-30");
        navbar.classList.add("shadow-xl");
        navbar.classList.remove("shadow-gray-700/40");
      } else {
        navbar.classList.remove("bg-purple-950/60");
        navbar.classList.remove("backdrop-filter");
        navbar.classList.remove("backdrop-blur-lg");
        navbar.classList.remove("bg-opacity-30");
        navbar.classList.remove("shadow-xl");
        navbar.classList.remove("shadow-gray-700/40");
      }
    }
  });
</script>

<!-- ChatGPT Shit

window.addEventListener("scroll", function () {
    const navbar = document.getElementById("navbar");
    const headerLink = document.getElementById("specificHeaderLink"); // Replace "specificHeaderLink" with the ID of your <headerlink>

    if (navbar) {
        if (window.scrollY > 0) {
            navbar.classList.add("bg-purple-950/60");
            navbar.classList.add("backdrop-filter");
            navbar.classList.add("backdrop-blur-lg");
            navbar.classList.add("bg-opacity-30");
            navbar.classList.add("shadow-xl");
            navbar.classList.remove("shadow-gray-700/40");

            // Check if the specific header link exists and apply a specific color
            if (headerLink) {
                headerLink.classList.add("text-red-500"); // Replace with your desired color class
            }
        } else {
            navbar.classList.remove("bg-purple-950/60");
            navbar.classList.remove("backdrop-filter");
            navbar.classList.remove("backdrop-blur-lg");
            navbar.classList.remove("bg-opacity-30");
            navbar.classList.remove("shadow-xl");
            navbar.classList.remove("shadow-gray-700/40");

            // Check if the specific header link exists and remove the specific color
            if (headerLink) {
                headerLink.classList.remove("text-red-500"); // Replace with the same color class used above
            }
        }
    }
});


 -->
